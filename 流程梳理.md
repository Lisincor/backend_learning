1.controller层拿到数据后, return Result.success(data)，然后前端vue中判断 Result.code 是否等于1，若等于1，则让table组件拿到数据（通过响应体中的data），然后渲染到前端界面

2.前端应该拿到的是Json格式的数据，springboot的注解@ResponseBody将返回值Result里的数据转换成Json，再响应给前端

![屏幕截图(1303)](C:\Users\19776\Pictures\Screenshots\屏幕截图(1303).png)